- name: Update system
  become: true
  pacman:
    update_cache: true
    upgrade: true

- name: Install packages
  become: true
  package:
    name: "{{ item }}"
    state: present
  with_items:
    - "{{ common }}"
  tags: [packages]